# Secrets — never push API keys
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# OS
.DS_Store
Thumbs.db

# Model artifacts are local/read-only; do not add new ones to git.
*.pkl
*.h5
*.tflite
*.npy
*.onnx
*.onnx.data
*.joblib

# YAMNet training data (large, fetched by scripts/fetch_audio_dataset.py)
data/audio_samples/
data/_cache/
backend/models/yamnet_head.joblib

# Acoustic baseline sidecars (rollback/comparison snapshots; canonical is panns_baseline.json)
backend/models/panns_baseline.flag_*.json
backend/models/panns_baseline_*.json
!backend/models/panns_baseline.json

# Web SPA local API config (copy from config.example.js)
web/config.js

# pytest / coverage
.coverage
.pytest_cache/

# Claude Code internal state
.claude/

# Runtime data — local SQLite + user-submitted feedback audio
*.db
data/feedback_clips/

# Frontend production build artifacts (scripts/build_frontend.py, snapshot-time)
scripts/.esbuild/
web/vendor/
